# JanSetu — Day 6 Parser Dependencies (MinerU / magic-pdf)
# Note: For production systems with GPU / CUDA acceleration, install this group:
# pip install -r requirements-parser.txt
#
# Core lightweight parser runtime dependencies (already included in requirements.txt):
# pymupdf>=1.24.0,<2.0.0
# pypdf>=4.0.0,<6.0.0

# Optional full MinerU / magic-pdf dependencies for deep layout analysis:
magic-pdf>=1.3.0
# torch>=2.2.0
# torchvision>=0.17.0
# transformers>=4.45.0
